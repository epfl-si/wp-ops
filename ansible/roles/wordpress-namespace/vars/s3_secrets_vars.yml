s3_bucket:
  os3:
    production:
      path: ""
      bucket_name: "{{ lookup('ansible.builtin.ini', 'bucket_name', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp') }}"
      keyId: "{{ lookup('ansible.builtin.ini', 'aws_access_key_id', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp') }}"
      accessSecret: "{{ lookup('ansible.builtin.ini', 'aws_secret_access_key', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp') }}"
  os4:
    production:
      path: ""
      bucket_name: "{{ lookup('ansible.builtin.ini', 'bucket_name', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-prod') }}"
      keyId: "{{ lookup('ansible.builtin.ini', 'aws_access_key_id', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-prod') }}"
      accessSecret: "{{ lookup('ansible.builtin.ini', 'aws_secret_access_key', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-prod') }}"
    test:
      path: ""
      bucket_name: "{{ lookup('ansible.builtin.ini', 'bucket_name', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-test') }}"
      keyId: "{{ lookup('ansible.builtin.ini', 'aws_access_key_id', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-test') }}"
      accessSecret: "{{ lookup('ansible.builtin.ini', 'aws_secret_access_key', file='/keybase/team/epfl_wp_prod/aws-cli-credentials', section='backup-wwp-os4-test') }}"

