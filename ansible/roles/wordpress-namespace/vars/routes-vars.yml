route_main_hostname: >-
  {{ "www.epfl.ch" if inventory_deployment_stage == "production"
     else "wpn-test.epfl.ch" }}

######## OpenShift 3 stuff (obsolescent) below

static_routes:
  wwp:
    - name: httpd-www
      hostname: www.epfl.ch
    - name: httpd-inside
      hostname: inside.epfl.ch
    - name: httpd-formation-wp
      hostname: formation-wp.epfl.ch
    # www-2018 has 'httpd-sandbox' service and not 'varnish-varnish'
    # - name: httpd-www2018
    #   hostname: www2018.epfl.ch

  wwp-test:
    - name: httpd-int
      hostname: migration-wp.epfl.ch
