---



- name: "Service"
  connection: local
  openshift:
    state: latest
    kind: Service
    name: "{{name}}"
    namespace: "{{ openshift_namespace }}"
    apiVersion: v1
    metadata:
        name: "{{name}}"
        labels:
          run: "{{name}}"
        namespace: "{{ openshift_namespace }}"
    spec:
        selector:
            run: "{{name}}"
        ports:
        - port: 8080
          protocol: TCP
          targetPort: 8080
  tags: svc
