---
# - include_vars: system_vars.yml
#   tags: always

- name: Config base
  tags:
    - system.config
    - system.config.base
  import_tasks:
    file: config_base.yml

- name: System Install
  tags:
    - system.install
  import_tasks:
    file: install.yml
